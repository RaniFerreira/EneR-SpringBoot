spring.application.name=ener

# Configuração da conexão com o banco de dados MySQL
spring.datasource.url=jdbc:mysql://localhost:3306/ener2?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=14122004

# Garante que o Hibernate crie ou atualize automaticamente o esquema do banco de dados com base no mapeamento das entidades
spring.jpa.hibernate.ddl-auto=update

# Habilita a exibição das consultas SQL no console, útil para depuração
spring.jpa.show-sql=true